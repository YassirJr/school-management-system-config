spring :
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sms_assignment_db
    username: root
    password: root
    jpa:
      database: mysql
      hibernate:
        ddl-auto: update
      show-sql: true
  data :
    rest :
      base-path : /api/v1
